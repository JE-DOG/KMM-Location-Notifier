# AGENTS

## Project Skills

When the task mentions Metro DI, `dev.zacsweers.metro`, `DependencyGraph`, or DI setup in Kotlin
Multiplatform:

- Automatically use the project skill at `.claude/skills/metro-di-mobile/SKILL.md` before
  implementation.
- Verify with module compile tasks before completion claims.
